import Button from "../common/Button";

const Header = () => {
  return (
    <header>
      <div></div>
      <nav>
        <Button>Login</Button>
      </nav>
    </header>
  );
};

export default Header;
